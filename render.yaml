services:
  - type: web
    name: mirror
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.ai_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /ai/risk/health
